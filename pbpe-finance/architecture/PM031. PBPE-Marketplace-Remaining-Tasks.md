次に進んでください。また、予定する残りの項目を知らせて下さい。

---

了解、次に進めます。  
まずは今のフェーズを一気に締めて、残りの「予定タスク」も一覧で出します。

---

### 1. PBPE Insurance API

**`docs/finance/PBPE_Insurance_Specification.md`** 済


# PBPE Insurance – Biosecurity × Finance

PBPE Insurance provides risk transfer products linked to:

- Biosecurity risk reduction
- Yield stability
- Environmental shocks

---

## 1. API Endpoints

- GET `/finance/insurance/products` – List insurance products
- POST `/finance/insurance/quote` – Get premium quote
- GET `/finance/insurance/policies` – List active policies

Premium:

$$
\text{Premium} = \phi_1 \cdot \text{BiosecurityRisk}
+ \phi_2 \cdot \text{YieldVolatility}
- \phi_3 \cdot \text{StabilityIndex}
$$

---


**`backend/models/insurance.py`**

```python
from pydantic import BaseModel
from typing import List


class InsuranceProduct(BaseModel):
    id: str
    name: str
    description: str
    coverage_type: str  # "biosecurity", "yield", "climate"


class InsuranceProductList(BaseModel):
    products: List[InsuranceProduct]


class InsuranceQuoteRequest(BaseModel):
    farm_id: str
    region: str
    biosecurity_risk: float
    yield_volatility: float
    stability_index: float
    coverage_type: str


class InsuranceQuoteResponse(BaseModel):
    premium_usd_per_year: float
    coverage_limit_usd: float
    deductible_usd: float


class InsurancePolicy(BaseModel):
    policy_id: str
    product_id: str
    premium_usd_per_year: float
    coverage_limit_usd: float
    status: str


class InsurancePolicyList(BaseModel):
    policies: List[InsurancePolicy]
```

**`backend/routers/insurance.py`**

```python
from fastapi import APIRouter
from models.insurance import (
    InsuranceProduct,
    InsuranceProductList,
    InsuranceQuoteRequest,
    InsuranceQuoteResponse,
    InsurancePolicy,
    InsurancePolicyList,
)

router = APIRouter()


@router.get("/insurance/products", response_model=InsuranceProductList)
def list_insurance_products():
    products = [
        InsuranceProduct(
            id="BIOSECURE-01",
            name="Biosecurity Stability Cover",
            description="Covers losses from disease outbreaks mitigated by MBT55.",
            coverage_type="biosecurity",
        ),
        InsuranceProduct(
            id="YIELD-01",
            name="Yield Stability Cover",
            description="Covers yield volatility under climate and soil stress.",
            coverage_type="yield",
        ),
    ]
    return InsuranceProductList(products=products)


@router.post("/insurance/quote", response_model=InsuranceQuoteResponse)
def get_insurance_quote(req: InsuranceQuoteRequest):
    base = 10000.0
    premium = (
        base
        + 2000.0 * req.biosecurity_risk
        + 1500.0 * req.yield_volatility
        - 3000.0 * req.stability_index
    )
    premium = max(premium, 1000.0)
    return InsuranceQuoteResponse(
        premium_usd_per_year=premium,
        coverage_limit_usd=500000.0,
        deductible_usd=5000.0,
    )


@router.get("/insurance/policies", response_model=InsurancePolicyList)
def list_policies():
    policies = [
        InsurancePolicy(
            policy_id="POL-0001",
            product_id="BIOSECURE-01",
            premium_usd_per_year=12000.0,
            coverage_limit_usd=500000.0,
            status="active",
        )
    ]
    return InsurancePolicyList(policies=policies)
```

**`backend/main.py` 追記**

```python
from routers import credits, impact, users, products, dashboard, bonds, insurance

app.include_router(insurance.router, prefix="/finance", tags=["insurance"])
```

---

### 2. Dashboard v3 UI 実装（主要ビュー）

**`frontend/src/api/dashboard.ts`**（すでに出したものの確定版）

```ts
import axios from "axios";

const api = axios.create({
  baseURL: "/api",
});

export const getDashboardSummary = () =>
  api.get("/dashboard/summary").then(res => res.data);

export const getGHGBreakdown = () =>
  api.get("/dashboard/ghg-breakdown").then(res => res.data);

export const getPBPEIssuance = () =>
  api.get("/dashboard/pbpe").then(res => res.data);

export const getFlywheel = () =>
  api.get("/dashboard/flywheel").then(res => res.data);

export const getCreditMarket = () =>
  api.get("/dashboard/credits/market").then(res => res.data);

export const getBondDashboard = () =>
  api.get("/dashboard/finance/bonds").then(res => res.data);

export const getEnterpriseUsage = () =>
  api.get("/dashboard/enterprise/usage").then(res => res.data);
```

**`frontend/src/pages/Dashboard/GlobalKPIs.tsx`**

```tsx
import React, { useEffect, useState } from "react";
import { getDashboardSummary } from "../../api/dashboard";
import { KPIcard } from "../../components/cards/KPIcard";

export default function GlobalKPIs() {
  const [summary, setSummary] = useState<any | null>(null);

  useEffect(() => {
    getDashboardSummary().then(setSummary);
  }, []);

  if (!summary) return null;

  return (
    <div className="kpi-row">
      <KPIcard
        label="GHG Reduction"
        value={`${summary.ghg_reduction_tco2e_per_year} tCO₂e / year`}
        subtext="Annual reduction"
      />
      <KPIcard
        label="PBPE Issued"
        value={summary.pbpe_issued_per_year}
        subtext="PBPE / year"
      />
      <KPIcard
        label="PBPE Market Value"
        value={`$${summary.pbpe_market_value_usd_per_year}`}
        subtext="USD / year"
      />
      <KPIcard
        label="Green Premium"
        value={`${summary.green_premium_jpy_per_year} JPY / year`}
        subtext="Negative premium"
      />
    </div>
  );
}
```

**`frontend/src/pages/Dashboard/MarketView.tsx`**

```tsx
import React, { useEffect, useState } from "react";
import { getCreditMarket } from "../../api/dashboard";
import { CreditsMarketTable } from "../../components/tables/CreditsMarketTable";

export default function MarketView() {
  const [market, setMarket] = useState<any | null>(null);

  useEffect(() => {
    getCreditMarket().then(setMarket);
  }, []);

  if (!market) return null;

  return (
    <div>
      <h2>PBPE Credits Market</h2>
      <CreditsMarketTable data={market.credits} />
    </div>
  );
}
```

**`frontend/src/pages/Dashboard/FinanceView.tsx`**

```tsx
import React, { useEffect, useState } from "react";
import { getBondDashboard } from "../../api/dashboard";
import { BondsTable } from "../../components/tables/BondsTable";

export default function FinanceView() {
  const [bonds, setBonds] = useState<any | null>(null);

  useEffect(() => {
    getBondDashboard().then(setBonds);
  }, []);

  if (!bonds) return null;

  return (
    <div>
      <h2>PBPE Finance – Bonds</h2>
      <BondsTable data={bonds.bonds} />
    </div>
  );
}
```

**`frontend/src/pages/Dashboard/EnterpriseView.tsx`**

```tsx
import React, { useEffect, useState } from "react";
import { getEnterpriseUsage } from "../../api/dashboard";

export default function EnterpriseView() {
  const [usage, setUsage] = useState<any | null>(null);

  useEffect(() => {
    getEnterpriseUsage().then(setUsage);
  }, []);

  if (!usage) return null;

  return (
    <div>
      <h2>Enterprise Adoption</h2>
      <ul>
        <li>Companies: {usage.companies_onboarded}</li>
        <li>Countries: {usage.countries}</li>
        <li>Sectors: {usage.sectors.join(", ")}</li>
        <li>Scope 3 Reports Linked: {usage.scope3_reports_linked}</li>
      </ul>
    </div>
  );
}
```

---

### 3. PBPE Credit Market モデル

**`docs/credits/PBPE_Credit_Market_Model.md`**


# PBPE Credit Market – Price Formation Model

PBPE Credits:

- Carbon
- Biosecurity
- Food Loss
- Quality
- Stability

---

## 1. Price Formation

$$
\text{Price} = \theta_1 \cdot \text{ImpactQuality}+ \theta_2 \cdot \text{Demand}+ \theta_3 \cdot \text{Liquidity}+ \theta_4 \cdot \text{VerificationLevel}
$$

- ImpactQuality: PBPE Credits Specification KPIs  
- Demand: enterprise Scope 3 demand  
- Liquidity: traded volume  
- VerificationLevel: MBT55 evidence + KPI + audit  

---

## 2. API Integration

- `GET /credits/price` – current prices  
- `GET /dashboard/credits/market` – market view  

PBPE Credit Market turns verified impact into a transparent, priceable asset class.


---

### 予定している残りの項目（この後に進められるもの）

**技術・実装寄り**

1. `backend/routers/credits.py` の価格形成ロジックを、上記モデルに合わせて微調整
2. Insurance API を OpenAPI (`openapi.yaml`) に追記
3. Dashboard v3 の残り UI（Flywheel 図、GHG グラフの本実装）
4. Frontend のルーティング（`App.tsx` / `react-router` など）定義
5. 簡易な認証フロー（/users/auth とフロントの接続）

**ドキュメント・外向け**

6. PBPE‑Finance 全体の 1 枚もの（投資家向け PDF 下書き）
7. PBPE OS Global Whitepaper（英語版の骨格）
8. PBPE Marketplace Pitch Deck v2（Dashboard v3 と Finance 反映）

どこから叩きにいくか、一言指定してくれれば、そのまま出力を続けます。

[[PM032. credits.py` の価格形成ロジック更新]]
