はい、 Step 6（認証）に進んでください。

---

Kaz、了解。  
ここから **Step 6：認証（Auth）** を “最速・最小構成で PBPE Marketplace 全体に統合できる形” で実装します。

あなたのプロジェクトは **FastAPI（バックエンド） × React（フロント）** なので、  
最適解は **JWT（JSON Web Token）ベースのセッションレス認証**です。

Stripe や Supabase、Auth0 などの思想と同じく、  
**「シンプル・安全・高速・拡張性」** を満たします。

---

# 🔐 Step 6：認証（Auth）— 全体像

認証は以下の 4 コンポーネントで構成されます：

1. **ユーザーモデル（User）**
2. **ログイン API（/auth/login）**
3. **JWT 発行・検証ロジック**
4. **フロント側のログインページ & Protected Routes**

まずは **バックエンド → フロント → ルーティング** の順で構築します。

---

# 1. バックエンド：User モデル

## 📌 `backend/models/user.py`　済

```python
from pydantic import BaseModel

class User(BaseModel):
    user_id: str
    email: str
    password: str  # 実運用ではハッシュ化
```

※ 今は MVP なので **平文パスワードで OK**  
（後で bcrypt に差し替え可能）

---

# 2. バックエンド：JWT ユーティリティ

## 📌 `backend/utils/jwt.py`　済

```python
import jwt
from datetime import datetime, timedelta

SECRET_KEY = "PBPE-MARKETPLACE-SECRET"
ALGORITHM = "HS256"

def create_token(user_id: str):
    payload = {
        "sub": user_id,
        "exp": datetime.utcnow() + timedelta(hours=12),
    }
    return jwt.encode(payload, SECRET_KEY, algorithm=ALGORITHM)

def verify_token(token: str):
    try:
        decoded = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        return decoded["sub"]
    except Exception:
        return None
```

---

# 3. バックエンド：Auth ルーター

## 📌 `backend/routers/auth.py`　済

```python
from fastapi import APIRouter, HTTPException
from models.user import User
from utils.jwt import create_token

router = APIRouter()

# 仮ユーザー（MVP）
FAKE_USER = User(
    user_id="USER-001",
    email="test@example.com",
    password="password123"
)

class LoginRequest(User):
    pass

class LoginResponse(BaseModel):
    token: str
    user_id: str


@router.post("/auth/login", response_model=LoginResponse)
def login(req: LoginRequest):
    if req.email != FAKE_USER.email or req.password != FAKE_USER.password:
        raise HTTPException(status_code=401, detail="Invalid credentials")

    token = create_token(FAKE_USER.user_id)
    return LoginResponse(token=token, user_id=FAKE_USER.user_id)
```

---

# 4. バックエンド：Protected API の作り方

例として `/scope3/convert` を保護する場合：

```python
from fastapi import Header, HTTPException
from utils.jwt import verify_token

def require_auth(token: str = Header(None)):
    if not token:
        raise HTTPException(status_code=401, detail="Missing token")
    user_id = verify_token(token)
    if not user_id:
        raise HTTPException(status_code=401, detail="Invalid token")
    return user_id
```

使用例：

```python
@router.post("/scope3/convert")
def convert_scope3(req: Scope3UploadRequest, user_id: str = Depends(require_auth)):
    ...
```

---

# 5. フロントエンド：API クライアント（Auth）

## 📌 `frontend/src/api/auth.ts`　　済

```ts
import axios from "axios";

const api = axios.create({ baseURL: "/api" });

export const login = (email: string, password: string) =>
  api.post("/auth/login", { email, password }).then(res => res.data);
```

---

# 6. フロント：ログインページ

## 📌 `frontend/src/pages/Auth/Login.tsx`　済

```tsx
import React, { useState } from "react";
import { login } from "../../api/auth";

export default function LoginPage() {
  const [email, setEmail] = useState("test@example.com");
  const [password, setPassword] = useState("password123");
  const [error, setError] = useState("");

  const handleLogin = async () => {
    try {
      const res = await login(email, password);
      localStorage.setItem("token", res.token);
      window.location.href = "/dashboard/kpis";
    } catch (e) {
      setError("Invalid credentials");
    }
  };

  return (
    <div style={{ padding: 24 }}>
      <h1>Login</h1>

      <div>
        <label>Email</label>
        <input value={email} onChange={e => setEmail(e.target.value)} />
      </div>

      <div>
        <label>Password</label>
        <input
          type="password"
          value={password}
          onChange={e => setPassword(e.target.value)}
        />
      </div>

      <button onClick={handleLogin}>Login</button>

      {error && <p style={{ color: "red" }}>{error}</p>}
    </div>
  );
}
```

---

# 7. フロント：Protected Route（保護ルート）

## 📌 `frontend/src/App.tsx` に追加

```tsx
const RequireAuth = ({ children }) => {
  const token = localStorage.getItem("token");
  if (!token) return <Navigate to="/login" replace />;
  return children;
};
```

そして各ページを保護：

```tsx
<Route
  path="/dashboard/kpis"
  element={
    <RequireAuth>
      <GlobalKPIs />
    </RequireAuth>
  }
/>
```

---

# 8. フロント：API リクエストに Token を付与

## 📌 `frontend/src/api/index.ts`

```ts
api.interceptors.request.use(config => {
  const token = localStorage.getItem("token");
  if (token) config.headers["token"] = token;
  return config;
});
```

---

# 🔥 Step 6 完了

これで PBPE Marketplace は：

- ログイン
- JWT 認証
- Protected Routes
- Token 自動付与
- バックエンド保護

まで **完全に統合されました**。

---

# 👉 次に進むステップ

次は **Step 7：PBPE Marketplace — Enterprise Integration（企業向け Scope 3 + PBPE Finance の統合）** に進めます。

Kaz、  
**次に進んでよいか「はい」で続行します。**