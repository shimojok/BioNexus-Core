お願いします。

---

Kaz、了解しました。  
ここでは **🅑 PBPE Financial Product Templates（JSON Schema）** を、  
Planetary Health OS と PBPE Marketplace の両方でそのまま使える **正式仕様** として完成させます。

あなたの pbpe-finance リポジトリの思想（Bond / Credit / Insurance / Protection / Health Impact / Agriculture / Climate）をすべて統合し、  
**PBPE-FIN-001 標準**として定義します。

以下は **そのまま GitHub の `/schemas/pbpe-financial-products.json` に置けるレベル**です。

---

# 🧩 **PBPE Financial Product Templates — JSON Schema（PBPE-FIN-001 完全版）**

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bionexus.earth/schemas/pbpe-financial-products.json",
  "title": "PBPE Financial Products Schema",
  "description": "Standardized JSON Schemas for PBPE-based financial products (PBPE-FIN-001 family).",
  "type": "object",

  "definitions": {

    "PBPEProductBase": {
      "type": "object",
      "required": [
        "product_id",
        "product_type",
        "underlying_intent",
        "pbpe_source",
        "expected_pbpe_yield",
        "risk_class",
        "duration_months",
        "payout_model",
        "distribution_model",
        "settlement_rules"
      ],
      "properties": {
        "product_id": {
          "type": "string",
          "description": "Unique identifier for the PBPE financial product."
        },
        "product_type": {
          "type": "string",
          "enum": ["bond", "credit", "insurance", "protection"],
          "description": "High-level category of the PBPE financial product."
        },
        "underlying_intent": {
          "type": "string",
          "enum": ["agri", "climate", "health"],
          "description": "Primary Intent type powering this product."
        },
        "pbpe_source": {
          "type": "string",
          "description": "Primary PBPE source metric (soil, co2, mbt55, medical_cost, yield, water)."
        },
        "expected_pbpe_yield": {
          "type": "number",
          "description": "Expected PBPE yield over the full duration."
        },
        "risk_class": {
          "type": "string",
          "enum": ["A", "B", "C", "D"],
          "description": "PBPE risk rating class."
        },
        "duration_months": {
          "type": "integer",
          "minimum": 1,
          "description": "Product duration in months."
        },
        "payout_model": {
          "type": "string",
          "enum": ["fixed", "variable", "hybrid"],
          "description": "Payout model for the product."
        },
        "distribution_model": {
          "type": "string",
          "description": "Value distribution model, typically '40/40/20'."
        },
        "currency": {
          "type": "string",
          "description": "Fiat currency used for pricing and settlement.",
          "examples": ["USD", "EUR", "JPY"]
        },
        "issuer": {
          "type": "string",
          "description": "Issuer identifier (organization, cooperative, government, etc.)."
        },
        "region": {
          "type": "string",
          "description": "Geographical region or jurisdiction."
        },
        "metadata": {
          "type": "object",
          "description": "Additional product metadata.",
          "additionalProperties": true
        },
        "settlement_rules": {
          "type": "object",
          "description": "Rules governing settlement, maturity, and PBPE realization.",
          "properties": {
            "maturity_condition": {
              "type": "string"
            },
            "pbpe_realization_method": {
              "type": "string"
            },
            "early_exit_policy": {
              "type": "string"
            }
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": false
    },

    "PBPECarbonIncomeBond": {
      "allOf": [
        { "$ref": "#/definitions/PBPEProductBase" },
        {
          "type": "object",
          "properties": {
            "product_type": { "const": "bond" },
            "underlying_intent": { "const": "climate" },
            "pbpe_source": { "enum": ["co2", "soil"] },
            "coupon_structure": {
              "type": "string",
              "description": "Coupon structure linked to PBPE (e.g., 'pbpe_indexed')."
            },
            "min_co2_reduction_tons": {
              "type": "number",
              "description": "Minimum CO₂ reduction required for full payout."
            }
          }
        }
      ]
    },

    "PBPERegenerativeAgricultureBond": {
      "allOf": [
        { "$ref": "#/definitions/PBPEProductBase" },
        {
          "type": "object",
          "properties": {
            "product_type": { "const": "bond" },
            "underlying_intent": { "const": "agri" },
            "pbpe_source": { "enum": ["soil", "yield", "water"] },
            "min_soc_increase_t_per_ha": {
              "type": "number",
              "description": "Minimum soil organic carbon increase per hectare."
            },
            "min_yield_improvement_percent": {
              "type": "number",
              "description": "Minimum yield improvement percentage."
            }
          }
        }
      ]
    },

    "PBPEHealthImpactBond": {
      "allOf": [
        { "$ref": "#/definitions/PBPEProductBase" },
        {
          "type": "object",
          "properties": {
            "product_type": { "const": "bond" },
            "underlying_intent": { "const": "health" },
            "pbpe_source": { "enum": ["mbt55", "medical_cost"] },
            "min_medical_cost_reduction": {
              "type": "number",
              "description": "Minimum medical cost reduction required for payout."
            },
            "target_population_size": {
              "type": "integer",
              "description": "Number of participants covered."
            }
          }
        }
      ]
    },

    "PBPECredit": {
      "allOf": [
        { "$ref": "#/definitions/PBPEProductBase" },
        {
          "type": "object",
          "properties": {
            "product_type": { "const": "credit" },
            "credit_type": {
              "type": "string",
              "enum": [
                "carbon_credit",
                "soil_carbon_credit",
                "coffee_resilience_credit",
                "microbiome_health_credit"
              ]
            },
            "unit_pbpe_value": {
              "type": "number",
              "description": "PBPE value per credit unit."
            },
            "unit_size": {
              "type": "number",
              "description": "Underlying physical unit (1 ton CO₂, 1 ha, 1 person-year)."
            }
          }
        }
      ]
    },

    "PBPEInsurance": {
      "allOf": [
        { "$ref": "#/definitions/PBPEProductBase" },
        {
          "type": "object",
          "properties": {
            "product_type": { "const": "insurance" },
            "insurance_type": {
              "type": "string",
              "enum": [
                "preventive_health_insurance",
                "productivity_protection",
                "agricultural_protection"
              ]
            },
            "coverage_scope": {
              "type": "string",
              "description": "Scope of coverage (health_costs, yield_loss, etc.)."
            },
            "trigger_conditions": {
              "type": "array",
              "items": { "type": "string" }
            }
          }
        }
      ]
    },

    "PBPEProtectionProduct": {
      "allOf": [
        { "$ref": "#/definitions/PBPEProductBase" },
        {
          "type": "object",
          "properties": {
            "product_type": { "const": "protection" },
            "protection_type": {
              "type": "string",
              "enum": [
                "coffee_resilience_protection",
                "crop_failure_protection",
                "climate_risk_protection"
              ]
            },
            "protected_asset": {
              "type": "string",
              "description": "Asset being protected (coffee_farm, rice_field, etc.)."
            },
            "max_payout": {
              "type": "number",
              "description": "Maximum payout amount."
            }
          }
        }
      ]
    }
  }
}
```

---

# 🎉 **PBPE Financial Product Templates（JSON Schema）完成**

これで PBPE Marketplace の金融商品は：

- **国際標準化可能な JSON Schema**
- **Planetary Health OS の Intent と完全連携**
- **Bond / Credit / Insurance / Protection の全カテゴリ対応**
- **PBPE-FIN-001 として統一**

という状態になりました。

---

# 🔥 次のステップ（順次作成）

次は **🅒 PBPE Marketplace の UI/UX モックアップ** を作成します。

- Investor Dashboard
- Issuer Dashboard
- Product Detail View
- PBPE Impact Visualization
- Settlement Flow UI

これらを **Figma / Web 実装にそのまま使えるレベル**で作成します。

続けます。

[[PM36. 🅒 PBPE Marketplace — UI_UX Mockups（完全版）]]
